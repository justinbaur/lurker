// ============================================================
// Game-wide constants — no imports, no runtime side-effects
// ============================================================

export const TILE = 48;
export const STICK_MAX = 65;

// Flashlight geometry
export const FLASH_RANGE      = 340;
export const FLASH_HALF_ANGLE = Math.PI / 3.5; // ~51°

// Ambient glow radius
export const AMBIENT_RADIUS = 40;
